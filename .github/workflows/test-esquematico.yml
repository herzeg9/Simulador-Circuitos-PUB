name: Test esquemático

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      update_snapshots:
        description: 'Atualizar PNGs de referência (regressão visual)'
        type: boolean
        default: false

jobs:
  audit:
    name: Auditoria layout SVG
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Instalar dependências
        run: npm install

      - name: Auditoria estrutural
        run: npm run test:esquematico

      - name: Snapshots SVG
        run: npm run test:svg

      - name: Regressão visual (screenshots PNG)
        run: npm run test:visual
        env:
          CI: 'true'
          UPDATE_SNAPSHOTS: ${{ github.event_name == 'workflow_dispatch' && inputs.update_snapshots && '1' || '' }}
